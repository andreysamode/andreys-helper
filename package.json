{
  "name": "andreys-helper",
  "displayName": "Andrey's Helper",
  "description": "Worktree controls in the Source Control panel for VSCode and Cursor, driving the worktrunk (wt) CLI headlessly — create and remove git worktrees without touching the terminal.",
  "version": "0.4.2",
  "publisher": "andrey",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/andreysamode/andreys-helper.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "SCM Providers",
    "Other"
  ],
  "keywords": [
    "worktree",
    "worktrunk",
    "wt",
    "git",
    "cursor"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "wt.worktreeMenu",
        "title": "Worktree Actions",
        "category": "Worktrunk",
        "icon": {
          "light": "media/tree-dark.svg",
          "dark": "media/tree-light.svg"
        }
      },
      {
        "command": "wt.newWorktree",
        "title": "New Worktree",
        "category": "Worktrunk",
        "icon": "$(git-branch)"
      },
      {
        "command": "wt.removeWorktree",
        "title": "Remove Worktree",
        "category": "Worktrunk",
        "icon": "$(trash)"
      },
      {
        "command": "andreys-helper.claudePanes1",
        "title": "Ensure 1 Pane",
        "category": "Claude Panes"
      },
      {
        "command": "andreys-helper.claudePanes2",
        "title": "Ensure 2 Panes",
        "category": "Claude Panes"
      },
      {
        "command": "andreys-helper.claudePanes3",
        "title": "Ensure 3 Panes",
        "category": "Claude Panes"
      },
      {
        "command": "andreys-helper.inspectClaudeTabs",
        "title": "Inspect Tabs (debug)",
        "category": "Claude Panes"
      },
      {
        "command": "andreysHelper.showIncomingWatch",
        "title": "Show Incoming Watched Files",
        "category": "Worktrunk",
        "icon": {
          "light": "media/tree-alert.svg",
          "dark": "media/tree-alert.svg"
        }
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "wt.worktreeMenu",
          "group": "navigation@-100",
          "when": "scmProvider == git && !andreysHelper.incomingWatchHit"
        },
        {
          "command": "andreysHelper.showIncomingWatch",
          "group": "navigation@-100",
          "when": "scmProvider == git && andreysHelper.incomingWatchHit"
        }
      ]
    },
    "configuration": {
      "title": "Andrey's Helper",
      "properties": {
        "andreysHelper.claudeCodePatch": {
          "type": "string",
          "enum": [
            "No change",
            "Patch",
            "Unpatch"
          ],
          "enumDescriptions": [
            "Take no action.",
            "Patch the installed Claude Code extension to enable worktree-scoped tabs and default the auto-include-file toggle off.",
            "Revert the Claude Code extension to its original, unpatched state (restores the pre-patch backup)."
          ],
          "default": "No change",
          "markdownDescription": "One-shot action to **patch or unpatch** the installed Claude Code bundle from within this extension — no shell script needed. Pick **Patch** or **Unpatch**; it runs, offers to restart the extension host to apply, then resets to *No change* (the actual patch state lives in the Claude bundle, not here). Safe to re-run and reversible."
        },
        "andreysHelper.startupPanes": {
          "type": "integer",
          "enum": [
            0,
            1,
            2,
            3
          ],
          "default": 0,
          "markdownDescription": "Automatically arrange Claude panes to this many columns when a window finishes starting up, replacing the restored layout. `0` disables it (default). The editor restores its previous layout first and this then re-arranges into the chosen column count — opening fresh Claude sessions if the window restored with none. Equivalent to pressing **C1/C2/C3** on startup."
        },
        "andreysHelper.closeCursorAgent": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When arranging Claude panes (C1/C2/C3), close Cursor's built-in agent view/tab first so it doesn't compete for editor space."
        },
        "andreysHelper.closeAgentEditorTabs": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Close Cursor's agent editor tabs when arranging Claude panes. Cursor renders its agent as an editor whose input type isn't exposed by the API; these are closed directly. Claude tabs are webviews and never affected. Requires `#andreysHelper.closeCursorAgent#`."
        },
        "andreysHelper.closeAuxiliaryBar": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Also close the auxiliary (secondary) side bar when arranging Claude panes — useful if your Cursor build docks the **Agents** panel there instead of as an editor tab. Off by default since it would also hide Claude's own secondary-sidebar view. Requires `#andreysHelper.closeCursorAgent#`."
        },
        "andreysHelper.cursorAgentViewTypePattern": {
          "type": "string",
          "default": "aichat|composer|cursor.*(chat|agent|pane)|(chat|agent).*pane",
          "markdownDescription": "Case-insensitive regular expression matched against editor webview `viewType`s to identify Cursor's agent/chat tab for closing. Claude's own tabs are always excluded. Use the **Claude Panes: Inspect Tabs (debug)** command to discover the real viewType."
        },
        "andreysHelper.cursorAgentCloseCommands": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Additional command IDs to run (if registered) to close Cursor's agent — useful when it's a side panel rather than an editor tab. Discover candidates with **Claude Panes: Inspect Tabs (debug)**."
        },
        "andreysHelper.watchIncoming.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Watch each open repo/worktree for **incoming commits** (what Sync would pull) that touch files matching `#andreysHelper.watchIncoming.patterns#`. While matches exist, the Source Control tree icon turns red; click it to list the files and open their incoming diffs."
        },
        "andreysHelper.watchIncoming.patterns": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "**/migrations/*.py",
            "!**/migrations/__init__.py"
          ],
          "markdownDescription": "Glob patterns matched against the repo-relative paths of incoming changed files. Prefix a pattern with `!` to exclude. Defaults to Django migrations; add any file or directory you want to be warned about before syncing (e.g. `config/settings.py`, `package-lock.json`)."
        },
        "andreysHelper.watchIncoming.fetchOnFocus": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Run one throttled `git fetch` per repo when the window regains focus, so the incoming-change indicator is fresh before you press Sync. Between focuses, detection rides on the git extension's own autofetch — no polling timer."
        },
        "andreysHelper.watchIncoming.focusFetchThrottleSeconds": {
          "type": "number",
          "default": 60,
          "markdownDescription": "Minimum seconds between focus-triggered fetches per repo. Requires `#andreysHelper.watchIncoming.fetchOnFocus#`."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "node esbuild.js --production",
    "watch": "node esbuild.js --watch",
    "compile": "tsc --noEmit",
    "test": "esbuild src/watchPatterns.test.ts --bundle --platform=node --format=cjs --outfile=dist-test/watchPatterns.test.cjs --log-level=warning && node --test dist-test/watchPatterns.test.cjs",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.20.0",
    "minimatch": "^10.2.5",
    "typescript": "^5.4.0"
  },
  "allowScripts": {
    "esbuild@0.20.2": true
  }
}
